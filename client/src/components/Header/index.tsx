import React from 'react';

import styles from './index.module.css';

const Header: React.FC = () => (
    <header>
        <h1 className={styles.heading}>Customer Feedback App</h1>
    </header>
);

export default Header;
