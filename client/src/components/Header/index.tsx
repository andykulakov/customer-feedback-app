import React from 'react';

import styles from './index.module.css';

const Header: React.FC = () => (
    <header>
        <h1 className={styles.heading}>Checkout.com Front End Challenge</h1>
    </header>
);

export default Header;
